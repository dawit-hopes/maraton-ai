version: '3.8'

services:
  chatbot-api:
    build: .
    container_name: hotel_chatbot_api
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app/data:/app/app/data